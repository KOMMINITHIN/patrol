Prompt 10 — Offline-First Reporting
Implement offline support for report creation.

Requirements:
- Detect offline state
- Queue reports locally
- Auto-sync when online
- Show sync status to user

Use browser APIs only (no new heavy libraries).