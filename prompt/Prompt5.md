Prompt 5 — Auth Flow Integration
Implement Google OAuth using Supabase Auth.

Requirements:
- Login/logout
- Persist auth state
- Auto-create profile row on first login
- Protect internal routes
- Graceful unauthenticated fallback (read-only mode)

Modify only frontend/src/auth and App.jsx where required.